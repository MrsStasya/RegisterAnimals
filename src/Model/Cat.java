package Model;

public class Cat extends Pets {

    public Cat (Integer id,String name, String type, String birthDate, String commands)
    {
        super(id, name, "Cat", birthDate, commands);
    }
}

